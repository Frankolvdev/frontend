# AI Virtual Try-On Frontend

Frontend público de la plataforma AI Virtual Try-On.

## Desarrollo local

1. Copia `.env.example` como `.env.local`.
2. Instala dependencias con `npm install`.
3. Inicia el proyecto con `npm run dev`.

El frontend usa Next.js, TypeScript y Tailwind CSS.
